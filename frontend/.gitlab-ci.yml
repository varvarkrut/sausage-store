default:
  cache:
    paths:
      - frontend/dist/frontend

variables:
    SNAPSHOT_VERSION: 1.0.${CI_PIPELINE_ID}

stages:
   - build
   - test
   - release
   - notify

include:
  - template: Security/SAST.gitlab-ci.yml

build:
   stage: build
   script:
     - cd frontend
     - npm install
     - npm run build
   only:
     changes:
     - frontend/*
   cache:
     paths:
       - frontend/dist/frontend


sonarqube-sast:
  stage: test
  only:
    changes:
      - frontend/*
  image: node:16.0.0-buster # тот самый docker-образ, о котором мы все узнаем в будущем
  script:
    - cd frontend
    - npm install -g sonarqube-scanner
    - >
      sonar-scanner
      -Dsonar.projectKey=${SONAR_FRONT_KEY}
      -Dsonar.sources=.
      -Dsonar.host.url=${SONAR_URL}
      -Dsonar.login=${SONAR_FRONT_LOGIN}
      -Dsonar.projectName=09_IVANSHKURKO_FRONTEND
  needs:
    - build



release:
   stage: release
   script:
     - cd frontend/dist
     - tar czvf sausage-store-${SNAPSHOT_VERSION}.tar.gz frontend
     - >
       curl -v -u "${NEXUS_REPO_USER}:${NEXUS_REPO_PASS}" --upload-file sausage-store-${SNAPSHOT_VERSION}.tar.gz ${NEXUS_REPO_URL}/10-sausage-store-shkurko-ivan-frontend/sausage-store/${SNAPSHOT_VERSION}/sausage-store-${SNAPSHOT_VERSION}.tar.gz
   only:
     changes:
     - frontend/*

notification:
  stage: notify
  only:
    changes:
    - frontend/*
  script: |
    ARTIFACT_URL=${NEXUS_REPO_URL}/10-sausage-store-shkurko-ivan-frontend/sausage-store/${SNAPSHOT_VERSION}/sausage-store-1.0.${SNAPSHOT_VERSION}.tar.gz
    curl -X POST -H "Content-type: application/json"  --data '{"text": " Вышла новая версия <'"$ARTIFACT_URL"'|frontend> сосичной '"$SNAPSHOT_VERSION"' "}' https://hooks.slack.com/services/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
  needs:
    - release

#deploy:
#  stage: deploy
#  before_script:
#    #устанавливаем ssh-agent для удобства аутентификации по ssh
#    - 'command -v ssh-agent >/dev/null || ( apt-get update -y && apt-get install openssh-client -y )'
#    - eval $(ssh-agent -s)
#    #сохраняем сгенеренный ранее приватный ключ для раннера
#    - echo "$SSH_PRIVATE_KEY" | tr -d '\r' | ssh-add -
#    - mkdir -p ~/.ssh
#    - chmod 600 ~/.ssh
#    - echo "$SSH_KNOWN_HOSTS" >> ~/.ssh/known_hosts
#    - chmod 644 ~/.ssh/known_hosts
#  script:
#    - scp ./frontend/sausage-store-backend.service ${DEV_USER}@${DEV_HOST}:/home/${DEV_USER}/sausage-store-frontend.service
#    - ssh ${DEV_USER}@${DEV_HOST} "export "CURRENT_VERSION=${SNAPSHOT_VERSION}"; export "VERSION=${SNAPSHOT_VERSION}"; export "DEV_HOST=${DEV_HOST}";export "NEXUS_REPO_URL=${NEXUS_REPO_URL}"; export "NEXUS_REPO_USER=${NEXUS_REPO_USER}"; export "NEXUS_REPO_PASS=${NEXUS_REPO_PASS}";setsid /bin/bash -s " < ./frontend/deploy.sh